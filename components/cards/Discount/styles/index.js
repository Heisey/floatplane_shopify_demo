import { Discount, Graphic } from './base'
import { backgroundGraphic, buttonContainer, cardBody, circle, toast } from './containers'
import { input } from './inputs'
import { contestRules, discountPrice, discountTitle } from './text' 

const styles = {
  Discount,
  Graphic,
  backgroundGraphic,
  buttonContainer,
  cardBody,
  circle,
  contestRules,
  discountPrice,
  discountTitle,
  input,
  toast
}

export default styles