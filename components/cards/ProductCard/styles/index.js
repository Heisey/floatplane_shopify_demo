import { ProductCard } from './base'
import { button } from './button'
import { contentContainer, imageContainer } from './containers'
import { productImage } from './images'
import { name, price } from './text'

const styles = {
  ProductCard,
  button,
  contentContainer,
  imageContainer,
  name,
  price,
  productImage
}

export default styles