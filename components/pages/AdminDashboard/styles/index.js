import { AdminDashboard, CardImageTop } from './base'
import { card, cardBody, cardContainer, cardFooter, cardInfoContainer, cardRow, cardSelectButton, cardTop, svgContainer } from './containers'
import { infoData, infoTitle, productName } from './text'

const styles = {
  AdminDashboard,
  CardImageTop,
  card,
  cardBody,
  cardContainer,
  cardFooter,
  cardInfoContainer,
  cardRow,
  cardSelectButton,
  cardTop,
  infoData,
  infoTitle,
  productName,
  svgContainer
}

export default styles