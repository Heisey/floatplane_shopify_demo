import { Card, ProductPage } from './base'
import { purchaseButton } from './buttons'
import { cardBody, cardContainer, cardContent, cardFooter, cardHeader, imageBox, priceContainer } from './containers'
import { productImage } from './images'
import { backgroundText, discount, dollar, name, price, subTitle } from './text'

const styles = {
  Card,
  ProductPage,
  backgroundText,
  cardBody,
  cardContainer,
  cardContent,
  cardFooter,
  cardHeader,
  discount,
  dollar,
  name,
  price,
  priceContainer,
  productImage,
  purchaseButton,
  subTitle,
  imageBox
}

export default styles